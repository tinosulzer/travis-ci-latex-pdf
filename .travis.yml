notifications:
  email: false
jobs:
  include:
      # Each script call is a separate job.
#    - stage: install
    - script: chmod +x $TRAVIS_BUILD_DIR/test/compile-2 && $TRAVIS_BUILD_DIR/test/compile-2
#    - stage: compile
#      script: ./2-texlive-pdflatex/compile.sh

cache:
  directories:
    - /tmp/texlive
    - $HOME/.texlive